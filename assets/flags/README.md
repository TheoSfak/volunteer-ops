# Flag icons

SVG flags (4x3 set) vendored from [lipis/flag-icons](https://github.com/lipis/flag-icons),
MIT licensed. Filenames are lowercase ISO 3166-1 alpha-2 codes (`gr.svg`, `it.svg`, ...),
matching `includes/countries.php`. Self-hosted rather than pulled from a CDN at runtime.
