<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Action Room live video. Capacitor's BridgeWebChromeClient already
         translates the WebView's VIDEO_CAPTURE / AUDIO_CAPTURE requests into
         runtime permission requests for exactly these three — but Android
         refuses instantly for any permission NOT declared here, so getUserMedia
         failed inside the APK while the same page worked in the mobile browser.
         The browser has its own permissions; the APK has only what this file
         grants it. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Lets the app offer its own downloaded update to the package installer.
         Without it a downloaded APK just sits there and the user has to go
         hunting through a file manager. The system still asks the user to
         allow installs from this source the first time. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- required="false" so the app still installs on a device with no camera.
         Declaring CAMERA above would otherwise make Google Play treat a camera
         as a hardware requirement and hide the app from such devices. Live
         video is one feature among many; it must not gate installation. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- @capgo/background-geolocation's own module manifest already declares
         everything the background GPS ping feature needs (location,
         FOREGROUND_SERVICE + FOREGROUND_SERVICE_LOCATION, POST_NOTIFICATIONS,
         WAKE_LOCK) — Android's manifest merger pulls those in automatically,
         nothing to duplicate here. Notably no ACCESS_BACKGROUND_LOCATION: the
         plugin runs via an active foreground service with a visible
         notification, which Android exempts from that permission entirely. -->
</manifest>
